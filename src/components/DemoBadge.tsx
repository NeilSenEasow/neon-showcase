export function DemoBadge() {
  return (
    <div className="fixed bottom-4 right-4 z-50 rounded-full glass neon-border px-4 py-2 text-xs font-medium text-primary">
      <span className="mr-1.5 inline-block h-2 w-2 animate-pulse-neon rounded-full bg-primary" />
      Digital Agency Demo Mode
    </div>
  );
}
